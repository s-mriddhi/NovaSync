import React, { useState } from "react";
import axios from "axios";
import styles from "./AddExpenseModal.module.css";

export default function AddExpenseModal({ groupId, token, onClose, onExpenseAdded }) {}
 //postman me try to register ten one by one feed the output aise ek baar me mat karo.